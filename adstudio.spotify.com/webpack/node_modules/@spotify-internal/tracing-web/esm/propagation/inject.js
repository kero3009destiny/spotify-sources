import { globalTracer, FORMAT_TEXT_MAP } from 'opentracing';
export function inject(ctx, tracer = globalTracer()) {
    const carrier = {};
    tracer.inject(ctx, FORMAT_TEXT_MAP, carrier);
    return carrier;
}
//# sourceMappingURL=inject.js.map