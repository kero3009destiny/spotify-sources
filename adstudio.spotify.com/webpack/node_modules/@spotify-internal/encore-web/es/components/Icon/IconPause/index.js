import React, { useContext } from 'react';
import { Icon } from "../";
import { Experimental__IconPause } from "../../Experimental__Icon/icons/Experimental__IconPause";
import { getStatus, encoreContextKeyword, encoreContextStatus, EncoreContext } from "../../../contexts/EncoreContext";
export function IconPause(props) {
  var config = useContext(EncoreContext);
  var status = getStatus(encoreContextKeyword.icon, config);

  if (status === encoreContextStatus.next) {
    return /*#__PURE__*/React.createElement(Experimental__IconPause, props);
  }

  return /*#__PURE__*/React.createElement(Icon, Object.assign({}, props, {
    dangerouslySetInnerHTML: {
      __html: "<rect x=\"5\" y=\"3\" width=\"4\" height=\"18\" fill=\"#181818\"/><rect x=\"15\" y=\"3\" width=\"4\" height=\"18\" fill=\"#181818\"/>"
    }
  }));
}
export default IconPause;