import styled, { css } from 'styled-components';
import { cssColorValue, focusBorderWidth, overlayColorTheme, semanticColors } from "../../styles";
import { List as ListComponent } from "../List";

/* Show up to 5.5 items, then resort to scrolling the rest.
 * We show a "half" item so that it is obvious to the user that there is more below the fold.
 * 28px (item height) * 5.5 + 1px (border height) = 155px
 * */
var focusStyles = function focusStyles(isUsingKeyboard) {
  return css(["outline:none;border:1px solid ", ";", ""], cssColorValue(semanticColors.essentialBase), isUsingKeyboard && css(["border-width:", "px;"], focusBorderWidth));
};

export var List = styled(ListComponent).withConfig({
  displayName: "List",
  componentId: "mygaxj-0"
})(["", ";display:block;pointer-events:all;width:100%;max-height:155px;overflow:auto;padding-top:0;padding-right:0;&:focus{", ";}", ";"], overlayColorTheme({
  maxWidth: 'none'
}), function (props) {
  return focusStyles(props.isUsingKeyboard);
}, function (props) {
  return props.isFocus && focusStyles(props.isUsingKeyboard);
});