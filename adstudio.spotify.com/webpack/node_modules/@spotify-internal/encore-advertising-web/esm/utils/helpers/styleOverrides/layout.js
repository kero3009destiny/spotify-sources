import { createParser } from './createParser';
export const layoutConfig = {
    width: ['width'],
    height: ['height'],
    minWidth: ['minWidth'],
    maxWidth: ['maxWidth'],
    minHeight: ['minHeight'],
    maxHeight: ['maxHeight'],
    display: ['display'],
    size: ['width', 'height'],
    verticalAlign: ['verticalAlign'],
    overflow: ['overflow'],
    overflowX: ['overflowX'],
    overflowY: ['overflowY'],
};
export const layoutOverrides = createParser(layoutConfig);
//# sourceMappingURL=layout.js.map