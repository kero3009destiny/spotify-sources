import { Type } from '@spotify-internal/encore-web';
import React from 'react';
import ReactMarkdown from 'react-markdown';
import { DEFAULT_ERROR_HELP_TEXT, RETRY_TEXT } from './constants';
import Container from './Container';
export default function PaymentMethodError({ errorHelpText = DEFAULT_ERROR_HELP_TEXT, error, hideShadow, noBorder, noPadding, onClickRetry, retryText = RETRY_TEXT, warning, }) {
    return (React.createElement(Container, { cta: { label: retryText, onClick: onClickRetry }, description: React.createElement(Type.span, { variant: Type.body2, weight: Type.book },
            React.createElement(ReactMarkdown, { source: errorHelpText, renderers: {
                    paragraph: ({ children }) => React.createElement("span", null, children),
                } })), error: !!error, hideShadow: hideShadow, noBorder: noBorder, noPadding: noPadding, warning: !!warning },
        React.createElement(Type.span, { variant: Type.body1, weight: Type.book }, error || warning)));
}
//# sourceMappingURL=Error.js.map