var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { gray70, Type } from '@spotify-internal/encore-web';
import secondsToTime from '../../utils/helpers/secondsToTime';
import styled from 'styled-components';
const Timecode = styled(Type.p) `
  width: fit-content;
  white-space: nowrap;
`;
export const PlaybackTimecode = (_a) => {
    var { time, duration, color = gray70, variant = Type.body2 } = _a, typeProps = __rest(_a, ["time", "duration", "color", "variant"]);
    const startTime = time !== undefined && secondsToTime(time);
    const endTime = duration !== undefined && secondsToTime(duration);
    const divider = !!startTime && !!endTime && ' / ';
    if (!startTime && !endTime)
        return null;
    return (React.createElement(Timecode, Object.assign({ condensed: true, variant: variant, color: color }, typeProps),
        startTime,
        divider,
        endTime));
};
//# sourceMappingURL=index.js.map