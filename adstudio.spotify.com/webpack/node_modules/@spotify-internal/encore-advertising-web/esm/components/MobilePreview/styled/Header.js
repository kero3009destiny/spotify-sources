import React from 'react';
import styled, { css } from 'styled-components';
import { Type, spacer16 } from '@spotify-internal/encore-web';
const HeaderWrapper = styled.div `
  display: flex;
  flex-direction: column;
  padding: 12px 0 8px;
  ${props => props.$withoutHeadline &&
    css `
      & p:first-of-type {
        padding-bottom: ${spacer16};
      }
    `}
`;
const InlineType = styled(Type.span) `
  display: inline;
`;
const createHeadlineTagLine = (headline, tagLine) => {
    let output;
    if (tagLine) {
        output = (React.createElement(Type.p, { variant: Type.body4, weight: Type.bold, condensed: true },
            React.createElement(InlineType, { variant: Type.body4, weight: Type.bold, condensed: true }, `${headline}: `),
            React.createElement(InlineType, { variant: Type.body4, weight: Type.book, condensed: true }, tagLine)));
    }
    else {
        output = (React.createElement(Type.p, { variant: Type.body4, weight: Type.bold, condensed: true }, headline));
    }
    return output;
};
export const Header = ({ title, headline, tagLine }) => (React.createElement(HeaderWrapper, { "$withoutHeadline": !headline },
    React.createElement(Type.p, { variant: Type.body4, weight: Type.book, condensed: !!headline }, title),
    createHeadlineTagLine(headline, tagLine)));
//# sourceMappingURL=Header.js.map