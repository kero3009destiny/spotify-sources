import React from 'react';
import styled from 'styled-components';
import { PlayPauseButton } from '../../PlayPauseButton';
import { white, gray20, IconSkipForward, IconSkipBack, IconSkipForward15, IconSkipBack15, } from '@spotify-internal/encore-web';
const StyledPlayPause = styled(PlayPauseButton) `
  color: ${white};
  margin: 0 24px;
  pointer-events: none;
`;
const ControlsWrapper = styled.div `
  display: flex;
  align-items: center;
  margin: 10px auto 0;
  justify-content: center;
`;
export const PlaybackControls = ({ showSkipControls, showPodcastControls = false, }) => {
    const skipControlColor = showSkipControls ? white : gray20;
    return (React.createElement(ControlsWrapper, null,
        showPodcastControls ? (React.createElement(IconSkipBack15, { color: gray20, iconSize: 24 })) : (React.createElement(IconSkipBack, { color: gray20 })),
        React.createElement(StyledPlayPause, { overMedia: true }),
        showPodcastControls ? (React.createElement(IconSkipForward15, { color: skipControlColor, iconSize: 24 })) : (React.createElement(IconSkipForward, { color: skipControlColor, direction: "next" }))));
};
//# sourceMappingURL=PlaybackControls.js.map