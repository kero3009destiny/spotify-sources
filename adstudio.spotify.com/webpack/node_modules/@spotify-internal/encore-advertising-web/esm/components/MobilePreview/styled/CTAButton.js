import React from 'react';
import styled from 'styled-components';
import { ButtonSecondary, white, black, spacer16, } from '@spotify-internal/encore-web';
const StyledButton = styled(ButtonSecondary) `
  background-color: ${white};
  border: none;
  color: ${black};
  margin-top: 15px;
  padding-left: ${spacer16};
  padding-right: ${spacer16};
  pointer-events: none;
`;
export const CTAButton = ({ ctaText }) => (React.createElement(StyledButton, { color: white, buttonSize: "sm" }, ctaText));
//# sourceMappingURL=CTAButton.js.map