import styled from 'styled-components';
import { formControlBase, formControlBaseSelect, formControlErrorTheme, formControlTheme, rootStyle } from "../../styles";

var dropdownTheme = function dropdownTheme(props) {
  var _props$theme;

  return ((_props$theme = props.theme) == null ? void 0 : _props$theme.dropdown) === 'dark' ? 'dark' : 'light';
};

export var Button = styled.button.withConfig({
  displayName: "Button",
  componentId: "sc-18tio8-0"
})(["", ";", ";", ";", ";", ";text-align:left;"], rootStyle(), formControlBase(), formControlBaseSelect(), function (props) {
  return formControlTheme({
    theme: dropdownTheme(props),
    isUsingKeyboard: props.isUsingKeyboard,
    error: props.error
  });
}, function (props) {
  return props.error && formControlErrorTheme({
    theme: dropdownTheme(props),
    isUsingKeyboard: props.isUsingKeyboard
  });
});