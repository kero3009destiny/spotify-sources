import { gray7, white } from '@spotify-internal/encore-foundation';
import styled from 'styled-components';
import { Type } from "../Type";
import { isDarkTheme } from "./";
export var Text = styled(Type).attrs(function (_ref) {
  var theme = _ref.theme;
  return {
    variant: Type.body2,
    color: isDarkTheme(theme) ? white : gray7
  };
}).withConfig({
  displayName: "Text",
  componentId: "sc-1i1cpvv-0"
})([""]);