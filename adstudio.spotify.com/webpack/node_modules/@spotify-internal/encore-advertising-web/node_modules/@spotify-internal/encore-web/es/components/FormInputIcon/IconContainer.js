import { spacer12 } from '@spotify-internal/encore-foundation';
import styled, { css } from 'styled-components';
import { formControlInputIconTheme } from "../../styles";
import Svg from "../Icon/Svg";

var iconTheme = function iconTheme(props) {
  var _props$theme;

  return ((_props$theme = props.theme) == null ? void 0 : _props$theme.formInputIcon) === 'dark' ? 'dark' : 'light';
};

function getPositionStyles(position) {
  var ltrProperty = position === 'trailing' ? 'right' : 'left';
  var rtlProperty = position === 'trailing' ? 'left' : 'right';
  return css(["", ":", ";[dir='rtl'] &{", ":auto;", ":", ";}"], ltrProperty, spacer12, ltrProperty, rtlProperty, spacer12);
}

export var IconContainer = styled.div.withConfig({
  displayName: "IconContainer",
  componentId: "ishj55-0"
})(["position:absolute;top:50%;transform:translateY(-50%);", ";", " ", "{display:block;}"], function (props) {
  return formControlInputIconTheme(iconTheme(props));
}, function (_ref) {
  var position = _ref.position;
  return getPositionStyles(position);
}, Svg);