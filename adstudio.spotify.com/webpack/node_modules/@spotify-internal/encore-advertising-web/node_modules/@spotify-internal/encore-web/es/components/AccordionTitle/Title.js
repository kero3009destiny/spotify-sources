import { black, spacer32, white } from '@spotify-internal/encore-foundation';
import styled, { css } from 'styled-components';
import { rootStyle } from "../../styles";

var textColor = function textColor(theme) {
  return theme.accordionTitle === 'dark' ? white : black;
};

var focusStyles = function focusStyles(theme, isUsingKeyboard) {
  var keyboardBorder = '3px';
  return css(["outline:none;border:", ";padding:", ";"], isUsingKeyboard ? "".concat(keyboardBorder, " solid ").concat(textColor(theme)) : 'none', isUsingKeyboard ? "calc(".concat(spacer32, " - ").concat(keyboardBorder, ")") : spacer32);
};

export var Title = styled.button.withConfig({
  displayName: "Title",
  componentId: "sc-12tkr16-0"
})(["", ";display:flex;align-items:center;justify-content:space-between;margin:0;background-color:transparent;border:0;width:100%;text-align:left;padding:", ";cursor:pointer;color:", ";&:focus{", ";}"], rootStyle(), spacer32, function (_ref) {
  var theme = _ref.theme;
  return textColor(theme);
}, function (_ref2) {
  var theme = _ref2.theme,
      isUsingKeyboard = _ref2.isUsingKeyboard;
  return focusStyles(theme, isUsingKeyboard);
});