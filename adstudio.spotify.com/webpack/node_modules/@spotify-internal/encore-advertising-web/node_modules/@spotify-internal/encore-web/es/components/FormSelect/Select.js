import styled from 'styled-components';
import { formControlBase, formControlBaseSelect, formControlErrorTheme, formControlTheme } from "../../styles";

var selectTheme = function selectTheme(props) {
  var _props$theme;

  return ((_props$theme = props.theme) == null ? void 0 : _props$theme.formSelect) === 'dark' ? 'dark' : 'light';
};

export var Select = styled.select.withConfig({
  displayName: "Select",
  componentId: "sc-1g6lqw8-0"
})(["", ";", ";", ";", ";"], formControlBase(), formControlBaseSelect(), function (props) {
  return formControlTheme({
    theme: selectTheme(props),
    isUsingKeyboard: props.isUsingKeyboard,
    error: props.error
  });
}, function (props) {
  return props.error && formControlErrorTheme({
    theme: selectTheme(props),
    isUsingKeyboard: props.isUsingKeyboard
  });
});