var makeCosmosBackwardCompatible = function (cosmos) {
    if (cosmos.sub === undefined) {
        cosmos.sub = function (url, onSuccess, onError) {
            cosmos.resolver.subscribe(url, function (error, response) {
                if (!error) {
                    var payload = response.getJSONBody();
                    onSuccess(payload);
                }
                else {
                    onError(error);
                }
            });
        };
    }
    if (cosmos.post === undefined) {
        cosmos.post = function (url, body) {
            cosmos.resolver.post({ url: url, body: body });
        };
    }
    return cosmos;
};
export { makeCosmosBackwardCompatible };
//# sourceMappingURL=CosmosBackwardCompatibityFitter.js.map