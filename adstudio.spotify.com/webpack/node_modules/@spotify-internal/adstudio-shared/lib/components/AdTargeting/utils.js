"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.generateAgeRange = generateAgeRange;

/**
 * Generate a range of numbers with the given boundaries.
 * @param {string} direction defined to be either start or end of age.
 * @param {number} from Lower bound, inclusive.
 * @param {number} to Upper bound, inclusive.
 * @returns {number[]} Array of objects, with properties 'key', the index of the object and 'value'
 * the integer between 'from' and 'end' for the index.  The first member of the array will have
 * 'key' = 0 and 'value' = 'from' and the last member of the array will have 'key' = srray.length
 * - 1 and 'value' = 'to'.
 */
function generateAgeRange(direction, from, to) {
  var range = [];

  for (var i = from; i <= to; i++) {
    range.push({
      key: i.toString(),
      value: direction === 'end' && i === to ? "".concat(i, "+") : i.toString()
    });
  }

  return range;
}