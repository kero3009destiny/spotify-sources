import { createParser } from './createParser';
export const positionConfig = {
    position: ['position'],
    zIndex: ['zIndex'],
    top: ['top'],
    right: ['right'],
    bottom: ['bottom'],
    left: ['left'],
};
export const positionOverrides = createParser(positionConfig);
//# sourceMappingURL=position.js.map