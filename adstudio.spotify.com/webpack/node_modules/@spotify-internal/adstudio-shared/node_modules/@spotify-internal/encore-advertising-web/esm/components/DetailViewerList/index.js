import React from 'react';
import styled, { css } from 'styled-components';
const DescriptionList = styled.dl `
  ${(props) => props.inline &&
    css `
      display: flex;
      width: 100%;
      padding-bottom: 0;
    `}
`;
export const DetailViewerList = ({ children, inline, className, }) => (React.createElement(DescriptionList, { className: className, inline: inline }, children));
DetailViewerList.defaultProps = {
    inline: false,
};
//# sourceMappingURL=index.js.map