import React from 'react';
import styled, { withTheme } from 'styled-components';
import { plum } from '../../styles/colors';
const offset = 187;
const duration = '2.4s';
const StyledSpinner = styled.svg `
  animation: rotator ${duration} linear infinite;
  && .pathIndeterminate {
    animation: dash ${duration} ease-in-out infinite;
    stroke-dasharray: ${offset};
    stroke-dashoffset: 0;
    transform-origin: 50% 50%;
  }
  @keyframes rotator {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(270deg);
    }
  }
  @keyframes dash {
    0% {
      stroke-dashoffset: ${offset};
    }
    50% {
      stroke-dashoffset: ${offset / 4};
      transform: rotate(135deg);
    }
    100% {
      stroke-dashoffset: ${offset};
      transform: rotate(450deg);
    }
  }
`;
const BaseSpinner = ({ diameter = '210px', theme }) => {
    var _a;
    return (React.createElement(StyledSpinner, { viewBox: "0 0 100 100", width: diameter, xmlns: "http://www.w3.org/2000/svg" },
        React.createElement("circle", { className: "pathIndeterminate", cx: "50", cy: "50", fill: "none", r: "25", stroke: ((_a = theme === null || theme === void 0 ? void 0 : theme.colors) === null || _a === void 0 ? void 0 : _a.primaryColor) ? theme.colors.primaryColor : plum, strokeLinecap: "round", strokeWidth: "2.5" })));
};
export const LoadingSpinner = withTheme(BaseSpinner);
//# sourceMappingURL=index.js.map