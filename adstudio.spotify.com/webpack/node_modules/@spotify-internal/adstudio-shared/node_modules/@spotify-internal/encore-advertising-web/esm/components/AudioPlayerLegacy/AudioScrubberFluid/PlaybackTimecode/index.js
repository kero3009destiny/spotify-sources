import React from 'react';
import styled from 'styled-components';
import { gray60, Type } from '@spotify-internal/encore-web';
import secondsToTime from '../../../../utils/helpers/secondsToTime';
const PlaybackTimecodeContainer = styled.div `
  color: ${props => props.$styleOptions && props.$styleOptions.color
    ? `${props.$styleOptions.color}`
    : `${gray60}`};
  display: flex;
  align-items: center;
  justify-content: ${props => props.$styleOptions && props.$styleOptions.position
    ? `${props.$styleOptions.position}`
    : 'space-between'};
`;
const Timecode = styled(Type) `
  padding: 2px;
  ${props => props.$styleOptions &&
    props.$styleOptions.fontSize &&
    `font-size: ${props.$styleOptions.fontSize}px`};
`;
export function PlaybackTimecode({ startTime, endTime, hideDivider = false, styleOptions, }) {
    return (React.createElement(PlaybackTimecodeContainer, { "$styleOptions": styleOptions },
        React.createElement(Timecode, { variant: Type.body3, weight: Type.book, "$styleOptions": styleOptions, condensed: true }, startTime !== undefined ? secondsToTime(startTime) : ''),
        !hideDivider && (React.createElement(Timecode, { variant: Type.body3, weight: Type.book, "$styleOptions": styleOptions, condensed: true }, "/")),
        React.createElement(Timecode, { variant: Type.body3, weight: Type.book, "$styleOptions": styleOptions, condensed: true }, endTime !== undefined ? secondsToTime(endTime) : '')));
}
//# sourceMappingURL=index.js.map