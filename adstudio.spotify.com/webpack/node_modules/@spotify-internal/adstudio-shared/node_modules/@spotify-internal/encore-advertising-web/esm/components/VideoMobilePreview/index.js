import React from 'react';
import { Type } from '@spotify-internal/encore-web';
import { VideoContainer, PreviewBody, Panel, HeaderWrapper, CTAButton, SkipControls, } from './styles';
import { ASPECT_RATIOS } from '../../config';
export const VideoMobilePreview = ({ title = 'Advertisement', headline, videoUrl, ctaText = 'Learn More', showPanel = true, aspectRatio = ASPECT_RATIOS.LANDSCAPE, skipText, }) => {
    const isLandscape = Number(aspectRatio) === ASPECT_RATIOS.LANDSCAPE;
    const header = (React.createElement(HeaderWrapper, { "$withoutHeadline": !headline },
        React.createElement(Type.p, { variant: Type.body4, weight: Type.book, condensed: !!headline }, title),
        React.createElement(Type.p, { variant: Type.body4, weight: Type.bold, condensed: true }, headline)));
    const body = (React.createElement(PreviewBody, { "$showPanel": showPanel },
        React.createElement(VideoContainer, { videoUrl: videoUrl, isLandscape: isLandscape }),
        header,
        React.createElement(CTAButton, { color: "white", buttonSize: "sm", "$isLandscape": isLandscape }, ctaText),
        React.createElement(SkipControls, { "$isLandscape": isLandscape },
            React.createElement(Type, { variant: Type.body4, weight: Type.bold }, skipText))));
    if (showPanel)
        return React.createElement(Panel, null, body);
    return body;
};
//# sourceMappingURL=index.js.map