import React from 'react';
import { Experimental__Icon as ExperimentalIcon } from '@spotify-internal/encore-web';
export const IconCircle = (props) => {
    const adjustedCircleSize = props.iconSize ? props.iconSize - 4 : 20;
    return (React.createElement(ExperimentalIcon, Object.assign({}, props, { dangerouslySetInnerHTML: {
            __html: `
        <circle
          cx="${props.iconSize ? props.iconSize / 2 : 12}"
          cy="${props.iconSize ? props.iconSize / 2 : 12}"
          r="${adjustedCircleSize / 2}"
          fill="${props.fill ? props.fill : 'none'}"
          stroke="${props.color}"
          strokeWidth="1"
          fillRule="evenodd"
        />`,
        } })));
};
//# sourceMappingURL=index.js.map