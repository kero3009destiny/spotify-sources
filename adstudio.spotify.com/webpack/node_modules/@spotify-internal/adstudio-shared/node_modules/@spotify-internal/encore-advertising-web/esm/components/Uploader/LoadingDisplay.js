import React from 'react';
import styled from 'styled-components';
import { Type, gray90, gray75, gray60 } from '@spotify-internal/encore-web';
import { Panel } from '../Panel';
import { UPLOADING, PROCESSING } from './constants';
const Container = styled.div `
  width: 100%;
`;
const TrackName = styled(Type.p) `
  padding-bottom: 10px;
  position: relative;
`;
const ProgressBarBackground = styled.div `
  background-color: ${gray90};
  border-radius: 2px;
  height: 4px;
  width: 100%;
`;
const ProgressBar = styled.div `
  background-color: ${gray60};
  border-radius: 2px;
  height: 4px;
  width: ${props => `${props.$progressInt || 0}%`};
`;
const StatusText = styled(Type.p) `
  padding-top: 10px;
`;
export default function LoadingDisplay({ name, progress, uploadingText = UPLOADING, processingText = PROCESSING, }) {
    const progressInt = Math.max(Math.min(progress, 100), 0);
    const statusText = progressInt === 100 ? `${processingText}...` : `${uploadingText}...`;
    return (React.createElement(Panel, { shadow: true },
        React.createElement(Container, null,
            React.createElement(TrackName, { variant: Type.body1, weight: Type.book }, name),
            React.createElement(ProgressBarBackground, null,
                React.createElement(ProgressBar, { "$progressInt": progressInt })),
            React.createElement(StatusText, { color: gray75, variant: Type.body3, weight: Type.book, condensed: true }, statusText))));
}
//# sourceMappingURL=LoadingDisplay.js.map