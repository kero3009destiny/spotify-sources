export default function secondsToTime(input) {
    const secs = Number(input);
    const hours = Math.floor(secs / 3600);
    const minutes = Math.floor((secs % 3600) / 60);
    const seconds = Math.floor((secs % 3600) % 60);
    const parts = [minutes, seconds];
    if (hours > 0) {
        parts.unshift(hours);
    }
    return parts
        .map((num, idx) => (idx === 0 ? num : num.toString().padStart(2, '0')))
        .join(':');
}
//# sourceMappingURL=secondsToTime.js.map