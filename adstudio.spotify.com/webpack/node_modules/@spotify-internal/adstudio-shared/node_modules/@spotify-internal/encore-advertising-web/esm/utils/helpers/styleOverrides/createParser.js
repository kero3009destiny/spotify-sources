export function createParser(propToCssPropDict = {}) {
    const parser = props => {
        let styles = {};
        Object.keys(props).forEach(key => {
            const cssProps = propToCssPropDict[key];
            if (!cssProps) {
                return;
            }
            const cssPropsStyles = cssProps.reduce((obj, prop) => {
                const value = props[key];
                return Object.assign(Object.assign({}, obj), { [prop]: value });
            }, {});
            styles = Object.assign(Object.assign({}, styles), cssPropsStyles);
        });
        return styles;
    };
    return parser;
}
//# sourceMappingURL=createParser.js.map