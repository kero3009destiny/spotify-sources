import React from 'react';
import { gray45 } from '@spotify-internal/encore-web';
import styled, { css } from 'styled-components';
export const ellipsisStyles = css `
  color: ${gray45};
  overflow-x: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;
const TruncateContainer = styled.div `
  display: flex;
  min-width: 200px;
`;
const Ellipses = styled.div `
  ${ellipsisStyles}
`;
export const renderTruncatedEmail = (email, delim) => {
    const parts = email.split(delim);
    const delimIndex = email.indexOf(delim);
    if (email.indexOf(delim) === -1) {
        return email;
    }
    return (React.createElement(TruncateContainer, { "data-testid": "truncate-container" },
        React.createElement(Ellipses, null, `${parts[0].slice(0, delimIndex)}`),
        React.createElement("div", null,
            " ",
            `${delim}${parts[1]}`,
            " ")));
};
//# sourceMappingURL=truncate.js.map