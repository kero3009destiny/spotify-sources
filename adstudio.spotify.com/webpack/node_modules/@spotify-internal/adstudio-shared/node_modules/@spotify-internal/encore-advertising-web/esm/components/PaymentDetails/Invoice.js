import React from 'react';
import ReactMarkdown from 'react-markdown';
import styled from 'styled-components';
import { DEFAULT_INVOICE_TEXT, DEFAULT_INVOICE_DESCRIPTION, DEFAULT_NONE_TEXT, DEFAULT_PO_NUMBER_TEXT, } from './constants';
import IconInvoice from '../../icons/Payment/IconInvoice';
import Container from './Container';
import { Type } from '@spotify-internal/encore-web';
const InvoiceType = styled(Type.p) `
  padding-top: 1em;
  padding-bottom: 4px;
`;
export default function Invoice({ invoiceDescription = DEFAULT_INVOICE_DESCRIPTION, invoiceText = DEFAULT_INVOICE_TEXT, noBorder, noPadding, noPONumberText = DEFAULT_NONE_TEXT, poNumberText = DEFAULT_PO_NUMBER_TEXT, poNumber, hideShadow, }) {
    return (React.createElement(Container, { hideShadow: hideShadow, noBorder: noBorder, noPadding: noPadding, icon: IconInvoice, description: React.createElement(ReactMarkdown, { source: invoiceDescription, renderers: {
                paragraph: ({ children }) => React.createElement("span", null, children),
            } }) },
        React.createElement(InvoiceType, { variant: Type.body2, weight: Type.bold }, invoiceText),
        React.createElement(Type.p, { variant: Type.body2, weight: Type.book },
            poNumberText,
            ": ",
            poNumber ? poNumber : noPONumberText)));
}
//# sourceMappingURL=Invoice.js.map