import React from 'react';
import { DetailsType } from './constants';
import AddCard from './AddCard';
import Card from './Card';
import Invoice from './Invoice';
import Loading from './Loading';
import PaymentMethodError from './Error';
export function PaymentDetails(props) {
    const { error, loading, type, warning } = props;
    if (loading)
        return React.createElement(Loading, Object.assign({}, props));
    if (error || warning)
        return React.createElement(PaymentMethodError, Object.assign({}, props));
    if (type === DetailsType.Card)
        return React.createElement(Card, Object.assign({}, props));
    if (type === DetailsType.Invoice)
        return React.createElement(Invoice, Object.assign({}, props));
    return React.createElement(AddCard, Object.assign({}, props));
}
export default PaymentDetails;
//# sourceMappingURL=index.js.map