"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = secondsToTime;

/**
 * add "0" to the front of every single digit value
 * except the very first item in the string, then join
 * with :. e.g. 4:33:33 or 18:22 or 22:19:05
 */
function secondsToTime(input) {
  var secs = Number(input);
  var hours = Math.floor(secs / 3600).toString();
  var minutes = Math.floor(secs % 3600 / 60).toString();
  var seconds = Math.floor(secs % 3600 % 60).toString(); // always include minutes and seconds - add hours if the length calls for it

  var parts = [minutes, seconds];

  if (hours > 0) {
    parts.unshift(hours);
  }

  return parts.map(function (num, idx) {
    return idx === 0 ? num : num.toString().padStart(2, '0');
  }).join(':');
}