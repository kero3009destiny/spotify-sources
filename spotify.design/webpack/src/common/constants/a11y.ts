/**
 * Main content identifier.
 * Used for "skip-to-main-content" a11y functionality.
 */
export const MAIN_CONTENT_ID = 'main-content';

/**
 * Application-wide top bar identifier.
 * Used to add to `document.blockingElements` when overlays are opened.
 */
export const TOP_BAR_ID = 'sd-topbar';

/**
 * Global classname to hide elements that rely on javascript.
 * gets set in the AppContext.
 */
export const HAS_JAVASCRIPT_CLASSNAME = 'has-javascript';
