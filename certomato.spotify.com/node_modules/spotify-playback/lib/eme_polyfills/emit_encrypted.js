'use strict';

/**
 * Emits a synthetic "encrypted" event for the player.
 */
module.exports = function() {
  var event = document.createEvent('Event');
  event.initEvent('encrypted', false, false);
  this.dispatchEvent(event);
};
