'use strict';

var debugEnum = require('spotify-debug-tools/debug_enum');

/**
 * Defines the different repeat modes.
 *
 * @enum {number}
 */
var RepeatMode = {
  /** Repeat off */
  OFF: 0,
  /** Repeat context */
  CONTEXT: 1,
  /** Repeat track */
  TRACK: 2
};

module.exports = debugEnum(RepeatMode);
