'use strict';

var debugEnum = require('spotify-debug-tools/debug_enum');

var ErrorSource = {
  CONNECT_API: 'connect-api',
  HARMONY: 'harmony',
  PLAYBACK: 'playback',
  TRACK_PLAYBACK: 'track-playback',
  TRANSPORT: 'transport'
};

module.exports = debugEnum(ErrorSource);
