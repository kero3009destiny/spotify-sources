import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import React from 'react';
import PropTypes from 'prop-types';
import TableElement from './Table';
import TableResponsive from './TableResponsive';
var propTypes = {
  /** Deprecated. Use TableContainer instead. */
  responsive: PropTypes.bool
};
export default function Table(_ref) {
  var responsive = _ref.responsive,
      props = _objectWithoutProperties(_ref, ["responsive"]);

  if (responsive) {
    /* eslint-disable-next-line no-console */
    console.warn('Warning: The `responsive` prop on the `Table` component is deprecated and will be removed soon. Use the `TableContainer` component instead. More information here: https://creator-tape.spotify.net/#!/TableContainer');
    return React.createElement(TableResponsive, null, React.createElement(TableElement, props));
  }

  return React.createElement(TableElement, props);
}
Table.propTypes = propTypes;