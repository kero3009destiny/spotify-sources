import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import React from 'react';
import PropTypes from 'prop-types';
import Dialog from './Dialog';
import Title from './Title';
import Footer from './Footer';
var propTypes = {
  /** Can accept any JSX */
  dialogTitle: PropTypes.node,

  /** Can accept any JSX */
  body: PropTypes.node,

  /** Can accept any JSX, meant for dismiss + accept buttons */
  footer: PropTypes.node
};
export default function DialogAlert(_ref) {
  var dialogTitle = _ref.dialogTitle,
      body = _ref.body,
      footer = _ref.footer,
      props = _objectWithoutProperties(_ref, ["dialogTitle", "body", "footer"]);

  return React.createElement(Dialog, props, React.createElement("header", null, React.createElement(Title, null, dialogTitle)), body, React.createElement(Footer, null, footer));
}
DialogAlert.propTypes = propTypes;