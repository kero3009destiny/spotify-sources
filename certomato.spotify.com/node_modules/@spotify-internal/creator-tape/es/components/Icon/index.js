import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import React from 'react';
import PropTypes from 'prop-types';
import { color as tokens } from '@spotify-internal/tokens/creator/web/tokens.common';
import Svg from './Svg';
var propTypes = {
  iconSize: PropTypes.oneOf([16, 24, 32, 48, 64]),

  /** Use a valid CSS color value, like `#000`, `rgba(0,0,0)`, or `black` */
  color: PropTypes.string,

  /** Deprecated. Use `Alt` icon components instead. */
  iconSolid: PropTypes.bool,

  /**
   * @ignore
   * Deprecated. Use `Alt` icon components instead
   */
  iconAlt: PropTypes.bool
};
var defaultProps = {
  iconSize: 24,
  color: 'currentColor'
};
/**
 *
 * @example ./ICONS.md
 */

export default function Icon(_ref) {
  var iconSize = _ref.iconSize,
      color = _ref.color,
      props = _objectWithoutProperties(_ref, ["iconSize", "color"]);

  return React.createElement(Svg, Object.assign({
    role: "img",
    height: iconSize,
    width: iconSize,
    iconSize: iconSize,
    viewBox: "0 0 24 24",
    iconColor: tokens[color] || color
  }, props));
}
Icon.propTypes = propTypes;
Icon.defaultProps = defaultProps;