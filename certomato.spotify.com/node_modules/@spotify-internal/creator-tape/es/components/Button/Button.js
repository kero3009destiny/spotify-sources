import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import React from 'react';
import styled from 'styled-components';
import { buttonBase } from '../../styles/mixins/buttons'; // eslint-disable-next-line prefer-arrow-callback

export default styled(function Button(_ref) {
  var _ref$component = _ref.component,
      Component = _ref$component === void 0 ? 'button' : _ref$component,
      variantRules = _ref.variantRules,
      buttonSize = _ref.buttonSize,
      condensed = _ref.condensed,
      props = _objectWithoutProperties(_ref, ["component", "variantRules", "buttonSize", "condensed"]);

  return React.createElement(Component, props);
}).withConfig({
  displayName: "Button",
  componentId: "kdidm1-0"
})(["", ";"], function (props) {
  return buttonBase({
    buttonSize: props.buttonSize,
    condensed: props.condensed,
    variantRules: props.variantRules
  });
});