import { Resolver } from '@spotify-internal/remote-config-resolver';
export const properties = {
    version: '1.0.0',
    properties: [
        {
            name: 'show_autocomplete',
            description: 'Show autocomplete instead of full form',
            boolSpec: {
                default: false,
            },
        },
    ],
};
const clientId = 'address-collection-sdk';
export default (options) => new Resolver(Object.assign({ clientId,
    properties }, options));
//# sourceMappingURL=createRemoteConfigResolver.js.map