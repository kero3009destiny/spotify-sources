const debounce = (fn, timeInMs) => {
    let timeout = null;
    return (...args) => {
        clearTimeout(timeout);
        return new Promise(resolve => {
            timeout = setTimeout(() => resolve(fn(...args)), timeInMs);
        });
    };
};
export default debounce;
//# sourceMappingURL=debounce.js.map