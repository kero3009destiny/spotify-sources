import { useFormikContext } from 'formik';
export default function FormikSubmitter(props) {
    const { submitForm } = useFormikContext();
    props.submitRef.current = submitForm;
    return null;
}
//# sourceMappingURL=FormikSubmitter.js.map