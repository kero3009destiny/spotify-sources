import React from 'react';
import { Field } from 'formik';
import { FormGroup, FormHelpText, FormSelect, } from '@spotify-internal/encore-web';
import styled from 'styled-components';
const SelectWrapper = styled.div `
  margin-top: 16px !important;
`;
export const SelectField = props => {
    const id = `address-${props.name}`;
    return (React.createElement(Field, { name: props.name }, ({ field, meta }) => {
        return (React.createElement(React.Fragment, null, props.useEncoreStyle ? (React.createElement(FormGroup, { label: props.label, labelFor: id },
            React.createElement(FormSelect, Object.assign({ id: id }, field, { error: meta.touched && meta.error !== undefined }), props.children),
            meta.touched && meta.error && (React.createElement(FormHelpText, { error: true }, meta.error)))) : (React.createElement(SelectWrapper, { className: "static-label-form-group" },
            React.createElement("select", Object.assign({ id: id, "aria-label": props.label }, field, { className: `${meta.touched && meta.error !== undefined
                    ? 'invalid-input'
                    : ''}` }), props.children),
            meta.touched && meta.error && (React.createElement("div", null,
                React.createElement("p", { className: "field-error" }, meta.error)))))));
    }));
};
//# sourceMappingURL=SelectField.js.map