import React from 'react';
import { Type } from '@spotify-internal/encore-web';
import styled from 'styled-components';
const Wrapper = styled.div `
  background: #f8f8f8;
  border: 1px solid #d9dadc;
  border-radius: 6px;
  padding: 10px;
  margin-top: 5px;
  font-size: 16px;
`;
export default function FormattedAddress({ address, country }) {
    let postalCodeExtra = undefined;
    if ('postal_code_extra' in address) {
        postalCodeExtra = address.postal_code_extra;
    }
    return (React.createElement(Wrapper, null,
        React.createElement(Type, { as: "p", condensed: true }, address.street),
        React.createElement(Type, { as: "p", condensed: true },
            address.city,
            ", ",
            address.state,
            " ",
            address.postal_code_short,
            postalCodeExtra && ` - ${postalCodeExtra}`),
        React.createElement(Type, { as: "p", condensed: true }, country)));
}
//# sourceMappingURL=FormattedAddress.js.map