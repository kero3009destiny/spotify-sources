import React from 'react';
import { Type } from '@spotify-internal/encore-web';
import styled from 'styled-components';
const LegalTermsWrapper = styled.div `
  margin-top: 40px;
`;
const LegalTerms = ({ legalTerms }) => {
    return (React.createElement(LegalTermsWrapper, null, legalTerms.map((legalTerm, i) => (React.createElement(Type.p, { key: i, dangerouslySetInnerHTML: { __html: legalTerm.html } })))));
};
export default LegalTerms;
//# sourceMappingURL=LegalTerms.js.map