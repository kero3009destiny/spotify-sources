import { TextLink, Type } from '@spotify-internal/encore-web';
import React from 'react';
export default function Header({ isStandalone, translations, onLearnMore, }) {
    return (React.createElement(React.Fragment, null,
        isStandalone ? (React.createElement(Type, { as: "h3", variant: Type.heading3 }, translations.enter_your_billing_address)) : (React.createElement(Type, { as: "p", weight: Type.bold }, "Address")),
        React.createElement(Type, { as: "p", variant: Type.body1 },
            React.createElement(TextLink, { onClick: e => {
                    e.preventDefault();
                    onLearnMore();
                }, href: "https://support.spotify.com/", target: "_blank" }, "Learn more"),
            ' ',
            "about why we need this information.")));
}
//# sourceMappingURL=Header.js.map