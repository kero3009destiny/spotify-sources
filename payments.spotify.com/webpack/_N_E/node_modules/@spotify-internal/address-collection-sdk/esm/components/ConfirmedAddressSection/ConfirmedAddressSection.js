import FormattedAddress from '../FormattedAddress';
import { CtaWrapper } from '../AddressCollection/components';
import { ButtonPrimary, TextLink } from '@spotify-internal/encore-web';
import { ActionType } from '../AddressCollection/reducer';
import React from 'react';
export default function ConfirmedAddressSection({ state, dispatch, country, isStandalone, }) {
    if (!state.confirmedAddress) {
        return null;
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(FormattedAddress, { address: state.confirmedAddress, country: country }),
        isStandalone ? (React.createElement(CtaWrapper, null,
            React.createElement(ButtonPrimary, { onClick: () => dispatch({
                    type: ActionType.SET_IS_EDITING,
                    payload: true,
                }) }, "Edit Address"))) : (React.createElement(TextLink, { href: "#", onClick: e => {
                e.preventDefault();
                dispatch({
                    type: ActionType.SET_IS_EDITING,
                    payload: true,
                });
            } }, "Edit Address"))));
}
//# sourceMappingURL=ConfirmedAddressSection.js.map