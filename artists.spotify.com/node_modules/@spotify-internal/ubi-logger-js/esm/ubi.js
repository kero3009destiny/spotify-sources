import { UBILogger } from './loggers/UBILogger';
export const UBI = (function fn() {
    let ubiLogger;
    function initUBILogger(eventSender) {
        return new UBILogger({ eventSender });
    }
    return {
        getUBILogger: function getUBILoggerFn(eventSender) {
            if (!ubiLogger) {
                ubiLogger = initUBILogger(eventSender);
            }
            return ubiLogger;
        },
    };
})();
//# sourceMappingURL=ubi.js.map