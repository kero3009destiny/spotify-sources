import styled, { css } from 'styled-components';
import { cssColorValue, fontWeightBold, semanticColors, textOverflow } from "../../styles";
import { Type } from "../Type";
export var Title = styled(Type).attrs({
  component: 'h3',
  variant: Type.body2,
  condensed: true
}).withConfig({
  displayName: "Title",
  componentId: "s602rk-0"
})(["font-weight:", ";display:block;color:", ";", ";"], fontWeightBold, cssColorValue(semanticColors.textBase), function (props) {
  return props.truncate && css(["", ";"], textOverflow());
});