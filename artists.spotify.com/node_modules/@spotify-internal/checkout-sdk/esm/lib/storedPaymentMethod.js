var PaymentProviderId;
(function (PaymentProviderId) {
    PaymentProviderId["StoredPaymentDetails"] = "billing_stored_details";
})(PaymentProviderId || (PaymentProviderId = {}));
export var hasStoredPaymentDetails = function (providers) {
    return providers.some(function (_a) {
        var id = _a.id;
        return id === PaymentProviderId.StoredPaymentDetails;
    });
};
export var retrieveStoredPaymentDetails = function (providers) {
    var storedPaymentProvider = providers.find(function (_a) {
        var id = _a.id;
        return id === PaymentProviderId.StoredPaymentDetails;
    });
    var storedPayment = false;
    var storedPaymentMethod = null;
    if (storedPaymentProvider) {
        storedPayment = true;
        var layout = storedPaymentProvider
            .layout[0];
        if (layout && layout.options.providerId) {
            storedPaymentMethod = layout.options.providerId;
        }
    }
    return [storedPayment, storedPaymentMethod];
};
