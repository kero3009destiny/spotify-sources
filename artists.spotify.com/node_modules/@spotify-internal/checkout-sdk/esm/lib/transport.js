export var returnDummyTransport = function () { return ({
    once: function () { },
    on: function () { },
    connect: function () { },
    isOnline: function () { return false; },
    isDummy: true,
}); };
export var isTransportEnabledForClient = function (clientName, clientContext) {
    if (clientContext === 'sdk-example') {
        return true;
    }
    return ['local'].indexOf(clientName) === -1;
};
