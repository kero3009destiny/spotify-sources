import googleAnalyticsDecorator from './googleAnalyticsPayloadDecorator';
import { trackGabito, trackGoogleAnalytics } from '../../api/dataTracking';
export var Trackers;
(function (Trackers) {
    Trackers[Trackers["Gabito"] = 2] = "Gabito";
    Trackers[Trackers["GoogleAnalytics"] = 3] = "GoogleAnalytics";
})(Trackers || (Trackers = {}));
export var trackEvent = function (event, trackers, clientName, environment) {
    if (trackers.indexOf(Trackers.Gabito) > -1) {
        trackGabito(event, clientName, environment);
    }
    if (trackers.indexOf(Trackers.GoogleAnalytics) > -1) {
        trackGoogleAnalytics(googleAnalyticsDecorator[event.eventName](event));
    }
};
