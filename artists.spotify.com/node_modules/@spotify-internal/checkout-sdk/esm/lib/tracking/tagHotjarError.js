var hasTriggeredHotjar = false;
export default (function () {
    if (window.hj && !hasTriggeredHotjar) {
        window.hj('trigger', 'checkout-error');
        window.hj('tagRecording', ['Error Shown']);
        hasTriggeredHotjar = true;
    }
});
