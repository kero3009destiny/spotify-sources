import { useEffect } from 'react';
export var formDataKeys = {
    providerId: 'checkout[stored_details][detailsProviderId]',
    detailsId: 'checkout[stored_details][detailsId]',
};
export default function StoredPaymentDetails(props) {
    useEffect(function () {
        props.setFormData(formDataKeys.providerId, props.providerId);
        props.setFormData(formDataKeys.detailsId, props.detailsId);
    }, []);
    return null;
}
