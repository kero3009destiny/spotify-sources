var mask = '00';
var regex = /^[0-9]{1,2}$/;
export var applyTwoDigitPinMask = function (inputText) {
    var text = inputText;
    if (text.length > mask.length) {
        return undefined;
    }
    if (text.length === 0) {
        return '';
    }
    var parts = text.match(regex);
    if (parts === null) {
        return undefined;
    }
    return parts[0];
};
export var validateTwoDigitPinMask = function (inputText, errorMessage) {
    if (regex.test(inputText) && inputText.length === mask.length) {
        return '';
    }
    return errorMessage;
};
