import React, { useContext } from 'react';
import StyledTertiaryButton from '../StyledTertiaryButton';
import CheckoutContext from '../CheckoutProvider';
import { FlowStep } from '../../types';
import { connect } from 'react-redux';
var mapStateToProps = function (state) { return ({
    flowStep: state.flowStep,
    changePaymentMethodLabel: state.strings.changePaymentMethodLabel,
}); };
var connector = connect(mapStateToProps);
export function ChangePaymentMethodButton(props) {
    var checkout = useContext(CheckoutContext.Context);
    return props.flowStep === FlowStep.Proceed ? (React.createElement(StyledTertiaryButton, { onClick: function () { return checkout.setProviderSelectionEnabled(); } }, props.changePaymentMethodLabel)) : null;
}
export default connector(ChangePaymentMethodButton);
