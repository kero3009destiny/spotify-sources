'use strict';

/**
 * A builder for UserAgent
 * NOTE: This code was generated and should not be changed
 *
 * @param {Object} data   The event data
 * @return {Object}   The validated and properly formatted data
 */
function createUserAgent(data) {
  var validated = data;
  if (process.env.DEBUG === 'true') {
    var validateSchema = require('spotify-debug-tools/debug_schema');
    validated = validateSchema(data, {
      value: {
        type: 'string',
        nullable: true,
      }
    });
  }

  return {
    name: 'context_user_agent',
    data: validated
  };
}

module.exports = createUserAgent;
