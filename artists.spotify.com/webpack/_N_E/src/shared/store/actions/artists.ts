// ignore-string-externalization
import { createAction } from 'redux-actions';
import { UNLOAD_CURRENT_ARTIST } from '../actionTypes';
export var unloadCurrentArtist = createAction(UNLOAD_CURRENT_ARTIST);